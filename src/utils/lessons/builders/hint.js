// One field of a quiz block's optional `feedback` object - see feedback.js.
// Shown only when the answer was wrong ("dica para memorizar").
export function hint(text) {
    return { hint: text };
}
