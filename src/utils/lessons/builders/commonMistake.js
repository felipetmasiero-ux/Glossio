// One field of a quiz block's optional `feedback` object - see feedback.js.
// Shown only when the answer was wrong ("erro comum").
export function commonMistake(text) {
    return { commonMistake: text };
}
