// One field of a quiz block's optional `feedback` object - see feedback.js.
// Shown only when the answer was correct ("curiosidade").
export function funFact(text) {
    return { funFact: text };
}
