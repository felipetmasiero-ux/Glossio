// Same three languages the app already supports (see getCoursesOverview.js,
// Profile.jsx, Grammar.jsx) - centralized here so the new flashcard/deck
// forms have one source instead of adding a fifth hardcoded copy.
export const SUPPORTED_LANGUAGES = ["English", "French", "Portuguese"];
