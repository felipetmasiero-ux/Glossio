// Sentinel used by both the deck filter UI (DecksBar) and filterFlashcards -
// distinguishes "no deck selected as a filter" (show everything) from
// "filtering for cards that have no deck assigned" (card.deckId === null).
export const NO_DECK_FILTER = "__no_deck__";
