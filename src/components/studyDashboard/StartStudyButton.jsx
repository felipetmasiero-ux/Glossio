import { Button } from "../common/Button/Button";

export function StartStudyButton({
    onClick
}) {

    return (

        <Button onClick={onClick}>

            Começar a estudar

        </Button>

    );

}
