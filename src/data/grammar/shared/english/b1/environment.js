// Reused verbatim from english/b1/environment.js's grammar block(s) - the lesson
// file imports this same constant instead of hardcoding the text a second time.
export const environmentBlocks = [
    {
        title: "First Conditional",
        text: "Use the first conditional to talk about a real, likely situation in the future and its result: if + present simple, ... will + verb. 'If we don't reduce plastic use, pollution will get worse.' The 'if' clause can come first or second, with a comma only when it comes first: 'We'll save energy if we turn off the lights.' Use 'unless' (meaning 'if not') for a negative condition: 'The planet will keep warming unless we act now.'"
    }
];
